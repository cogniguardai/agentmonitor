# Mythos workspace runtime deps (already installed in ai-env)
rich>=13.0
PyYAML>=6.0
loguru>=0.7
torch>=2.1
transformers>=4.40
einops>=0.8

# Local web GUI (app/main.py)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
httpx>=0.25
